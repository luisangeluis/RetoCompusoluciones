const reverseDate = (fecha) => {
  // console.log(fecha);
  // let info = fecha.split('/')
  // // console.log(info);
  // for (let i = 0; i < info.length; info++) {
  //   // if (info[i].length < 2)
  //   //   info[i] = `0${info[i]}`
  //   console.log(info[i].length); 
  // }
  
  // console.log(info);
  // return info.join('-');
  return fecha
}

export default reverseDate;